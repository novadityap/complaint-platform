services:
  server:
    image: novadityap/complaint-platform-server-dev
    container_name: complaint-platform-server-dev
    build:
      context: ./server
      dockerfile: Dockerfile
    command: >
      sh -c "
        php artisan migrate:fresh --seed --force && 
        php artisan serve --host=0.0.0.0
      "
    env_file:
      - ./server/.env
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    restart: always
    volumes:
      - ./server:/app
    networks:
      - complaint-platform-dev

  client:
    image: novadityap/complaint-platform-client-dev
    container_name: complaint-platform-client-dev
    build:
      context: ./client
      dockerfile: Dockerfile
    command: npm run dev
    env_file:
      - ./client/.env.development
    ports:
      - "3000:3000"
    restart: always
    volumes:
      - ./client:/app
    networks:
      - complaint-platform-dev

  postgres:
    container_name: complaint-platform-postgres-dev
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    volumes:
      - complaint-platform-postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: complaint_platform_dev
    networks:
      - complaint-platform-dev

volumes:
  complaint-platform-postgres-data:
    name: complaint-platform-postgres-data
networks:
  complaint-platform-dev:
    name: complaint-platform-dev
